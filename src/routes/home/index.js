import React from 'react';

import Logo from '../../components/logo'
import SearchBar from '../../components/search-bar'

export default () => {
  return (
    <div>
      <Logo/>
      <SearchBar />
    </div>
  )
};
