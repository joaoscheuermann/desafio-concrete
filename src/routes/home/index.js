import React from 'react';
import Logo from '../../components/logo'
import Button from '../../components/button'

export default () => {
  return (
    <div>
      <Logo/>
      <Button>
        
      </Button>
    </div>
  )
};
